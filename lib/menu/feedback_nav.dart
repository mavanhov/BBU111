import 'package:flutter/material.dart';

class FeedbackNav extends StatelessWidget {
  const FeedbackNav({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Feedback')),
      body: ListView(),
    );
  }
}
